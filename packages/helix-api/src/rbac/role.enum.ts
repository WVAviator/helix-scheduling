export enum Role {
  USER = 'user', //User is an employee who view their schedule and enter requests
  MANAGER = 'manager', //Managers can approve requests for employees
  ADMIN = 'admin', //Admin can add or remove employees from the company
}
